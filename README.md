# gocli

A simple Go CLI application. All in one file.

## Usage

```bash
go run main.go greet [name]
go run main.go calc <add|sub|mul|div|mod> <a> <b>
go run main.go upper <string>
go run main.go version
go run main.go help
```
